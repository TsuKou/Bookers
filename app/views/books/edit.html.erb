<h1>Edting Book</h1>

<%= form_with model: @book, url: book_path(@book.id), method: :patch do |f| %>

<div class="label_G">
  <label>
    <div class="t_title">Title</a><br>
    <%= f.text_field :title %><br>
  </label>
</div>

<div class="label_G">
  <label>
    Body<br>
    <%= f.text_area :body %><br>
  </label>
</div>

<div class="create_bt">
  <%= f.submit 'Update Book' %>
</div>

<% end %>

<%= link_to "Show", "/books/#{@book.id}", class: "show_#{@book.id}" %>

|

<%= link_to "Back", "/books" %>